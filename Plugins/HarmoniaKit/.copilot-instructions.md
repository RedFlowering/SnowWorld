````instructions
# HarmoniaKit 플러그인 - AI 세션 참고 문서

> 이 문서는 AI 어시스턴트가 HarmoniaKit 플러그인 작업 시 참고하는 요약 문서입니다.

---

## 📑 목차

1. [프로젝트 개요](#-프로젝트-개요)
2. [개발 환경](#️-개발-환경)
3. [플러그인 의존성](#-플러그인-의존성)
4. [플러그인 구조](#️-플러그인-구조)
5. [모듈 구조](#-모듈-구조)
6. [핵심 시스템 요약](#-핵심-시스템-요약)
7. [주요 인터페이스 경로](#-주요-인터페이스-경로)
8. [주요 데이터 구조체](#-주요-데이터-구조체)
9. [코딩 컨벤션](#-코딩-컨벤션)
10. [작업 시 참고사항](#-작업-시-참고사항)
11. [빠른 코드 예시](#-빠른-코드-예시)
12. [상세 문서 위치](#-상세-문서-위치)
13. [알려진 이슈 및 제한사항](#️-알려진-이슈-및-제한사항)

---

## 📌 프로젝트 개요

| 항목 | 값 |
|------|-----|
| **프로젝트명** | SnowWorld (Lyra 기반) |
| **플러그인명** | HarmoniaKit |
| **엔진 버전** | Unreal Engine 5.7 |
| **플러그인 경로** | `C:\Projects\SnowWorld\Plugins\HarmoniaKit` |
| **Lyra 소스 경로** | `C:\Projects\SnowWorld\Source\LyraGame` |

---

## 🛠️ 개발 환경

### 엔진 경로

```
C:\Epic\UE_5.7
```

### VS Code 빌드 태스크

| 태스크 | 설명 |
|-------|------|
| `LyraEditor Win64 Development Build` | 에디터 개발 빌드 (일반 개발 시 사용) |
| `LyraGame Win64 Development Build` | 게임 개발 빌드 |
| `LyraClient Win64 Development Build` | 클라이언트 빌드 |
| `LyraServer Win64 Development Build` | 서버 빌드 |

> 태스크 실행: `Ctrl+Shift+B` 또는 `Terminal > Run Build Task`

### 주요 파일 위치

| 파일 | 경로 |
|------|------|
| 프로젝트 파일 | `C:\Projects\SnowWorld\SnowWorld.uproject` |
| 엔진 소스 | `C:\Epic\UE_5.7\Engine\Source` |
| 게임플레이 태그 | `Config\DefaultGameplayTags.ini` |
| 빌드 태스크 | `.vscode\tasks.json` |

---

## 🔗 플러그인 의존성

### Lyra 프레임워크 (필수)

> ⚠️ **중요**: HarmoniaKit은 **Lyra Starter Game** 기반 프로젝트입니다.

| 모듈 | 용도 | 비고 |
|------|------|------|
| `LyraGame` | 게임플레이 기본 프레임워크 | 어빌리티, 캐릭터, 장비 시스템 기반 |

**Lyra 연동 클래스**:
- `UHarmoniaAttributeSet` → `ULyraAttributeSet` 확장
- `UHarmoniaLyraEquipmentAdapter` → Lyra 장비 시스템 어댑터
- 모든 GameplayAbility → `ULyraGameplayAbility` 기반

### 필수 플러그인

| 플러그인 | 용도 |
|----------|------|
| `CommonUI` | UI 시스템 |
| `GameplayAbilities` | GAS (Gameplay Ability System) |
| `OnlineSubsystem` | 온라인 기능 기본 |
| `OnlineSubsystemEOS` | Epic Online Services |
| `OnlineSubsystemSteam` | Steam 통합 |
| `OnlineSubsystemUtils` | 온라인 유틸리티 |
| `SenseSystem` | 멀티스레드 히트 감지 |
| `ALS` | Advanced Locomotion System |

### 추가 엔진 모듈 의존성

| 모듈 | 용도 |
|------|------|
| `EnhancedInput` | 향상된 입력 시스템 |
| `NavigationSystem` | AI 내비게이션 |
| `AIModule` | AI 시스템 |
| `Niagara` | VFX 시스템 |
| `LevelSequence`, `MovieScene` | 컷신 시스템 |

---

## 🏗️ 플러그인 구조

```
HarmoniaKit/
├── Source/
│   ├── HarmoniaKit/           # 메인 런타임 모듈
│   │   ├── Public/
│   │   │   ├── Abilities/     # GAS 어빌리티 (Dodge, Block, Parry 등)
│   │   │   ├── AbilitySystem/ # AttributeSet, AbilitySystemLibrary
│   │   │   ├── Actors/        # 게임 액터 (Projectile, Spawner 등)
│   │   │   ├── AI/            # BT 태스크, 서비스, AI 컨트롤러
│   │   │   ├── Animation/     # AnimNotify, AnimNotifyState
│   │   │   ├── Components/    # 모든 컴포넌트 (전투, 생활, AI 등)
│   │   │   ├── Data/          # 데이터 에셋 (ClassData, SkillTree 등)
│   │   │   ├── Definitions/   # Enum, Struct 정의 파일들
│   │   │   ├── Equipment/     # 장비 시스템 (Lyra 어댑터)
│   │   │   ├── Interfaces/    # 인터페이스 (Interactable, Admin)
│   │   │   ├── Inventory/     # 인벤토리 프래그먼트
│   │   │   ├── Libraries/     # 유틸리티 함수 라이브러리
│   │   │   ├── Managers/      # 월드 매니저 (Dungeon, Building 등)
│   │   │   ├── Monsters/      # 몬스터 기본 클래스 및 인터페이스
│   │   │   ├── Settings/      # 설정 클래스 (TagSettings)
│   │   │   ├── System/        # 서브시스템 (Save, Team, Quest 등)
│   │   │   ├── UI/            # UI 위젯 (Inventory, Map, Quest)
│   │   │   └── Utils/         # 헬퍼 유틸리티
│   │   └── Private/           # 구현 파일 (.cpp)
│   ├── HarmoniaEditor/        # 에디터 전용 모듈
│   ├── HarmoniaLoadManager/   # 데이터 로딩/저장 관리
│   ├── HarmoniaOnlineSubsystem/ # Steam/EOS 통합
│   ├── HarmoniaStory/         # 대화/컷신/저널/관계 시스템
│   ├── HarmoniaWorldGenerator/ # 절차적 월드 생성
│   ├── HarmoniaModSystem/     # 모드 지원
│   ├── HarmoniaLocalizationSystem/ # 다국어 지원
│   └── HarmoniaGameService/   # 플랫폼 서비스 (업적, 클라우드)
├── Content/                   # 블루프린트, 데이터 애셋
├── Docs/                      # 문서
│   ├── HarmoniaKit_Complete_Documentation.md
│   ├── Systems/               # 시스템별 상세 문서
│   ├── GettingStarted/        # 빠른 시작 가이드
│   └── Refactoring/           # 리팩토링 기록
└── HarmoniaKit.uplugin        # 플러그인 설정
```

---

## 📦 모듈 구조

| 모듈명 | 타입 | 설명 |
|--------|------|------|
| `HarmoniaKit` | Runtime | 메인 게임플레이 모듈 |
| `HarmoniaEditor` | Editor | 에디터 전용 도구 |
| `HarmoniaLoadManager` | Runtime | 데이터 로딩/저장 관리 |
| `HarmoniaLoadManagerEditor` | Editor | 데이터 에디터 도구 |
| `HarmoniaOnlineSubsystem` | Runtime | Steam/EOS 온라인 통합 |
| `HarmoniaStory` | Runtime | 대화, 저널, 컷신, 관계 시스템 |
| `HarmoniaWorldGenerator` | Runtime | 절차적 월드 생성 |
| `HarmoniaModSystem` | Runtime | 모드 지원 및 에셋 오버라이드 |
| `HarmoniaLocalizationSystem` | Runtime | 다국어 지원 |
| `HarmoniaGameService` | Runtime | 플랫폼 서비스 통합 (업적, 클라우드 저장) |

---

## 🎮 핵심 시스템 요약

### 1. 전투 시스템

| 컴포넌트 | 역할 | 파일 |
|---------|------|------|
| `UHarmoniaBaseCombatComponent` | 전투 기본 클래스 | `HarmoniaBaseCombatComponent.h` |
| `UHarmoniaMeleeCombatComponent` | 근접 전투 | `HarmoniaMeleeCombatComponent.h` |
| `UHarmoniaSenseAttackComponent` | 히트 감지 | `HarmoniaSenseAttackComponent.h` |
| `UHarmoniaGameplayAbility_ComboAttack` | 콤보 어빌리티 | `HarmoniaGameplayAbility_ComboAttack.h` |
| `UHarmoniaLockOnComponent` | 락온 타게팅 | `Components/HarmoniaLockOnComponent.h` |
| `UHarmoniaGameplayAbility_Dodge` | 회피 구르기 | `Abilities/HarmoniaGameplayAbility_Dodge.h` |

**주요 기능**:
- 11종 무기 타입 지원
- GAS 기반 어빌리티 시스템
- SenseSystem 기반 멀티스레드 히트 감지
- 패리, 회피, 리포스트, 백스탭
- **락온 타게팅**: 자동 타겟 찾기, 타겟 전환, 자동 카메라 회전
- **회피 구르기**: Light/Medium/Heavy 롤 타입, 무적 프레임(I-Frame)

### 2. 캐릭터 시스템

| 컴포넌트 | 역할 |
|---------|------|
| `UHarmoniaAttributeSet` | GAS AttributeSet (Health, Stamina, Mana, Poise 등) |
| `UHarmoniaRechargeableItemComponent` | 충전식 회복 아이템 (에스투스 스타일) |
| `UHarmoniaCurrencyManagerComponent` | 통화(Souls) 관리 |
| `UHarmoniaDeathPenaltyComponent` | 사망 패널티 (혈흔 시스템) |

### 3. 생활 컨텐츠 시스템

| 컴포넌트 | 역할 |
|---------|------|
| `UHarmoniaFishingComponent` | 낚시 시스템 |
| `UHarmoniaGatheringComponent` | 채집 시스템 |
| `UHarmoniaCookingComponent` | 요리 시스템 |
| `UHarmoniaFarmingComponent` | 농사 시스템 |
| `UHarmoniaMusicComponent` | 음악/연주 시스템 |

### 4. 온라인 및 팀 시스템

| 클래스 | 역할 |
|--------|------|
| `UHarmoniaOnlineSubsystem` | Steam/EOS 통합 |
| `UHarmoniaTeamManagementSubsystem` | 동적 팀/진영 관리 |
| `IHarmoniaTeamAgentInterface` | 팀 인터페이스 |

### 5. 기타 주요 시스템

| 시스템 | 핵심 클래스 |
|--------|------------|
| 퀘스트 | `UHarmoniaQuestComponent`, `FQuestData` |
| 던전/레이드 | `UHarmoniaDungeonComponent`, `FHarmoniaDungeonData` |
| 제작 | `UHarmoniaCraftingComponent`, `FCraftingRecipeData` |
| 저장 | `UHarmoniaSaveGameSubsystem`, `IHarmoniaSaveInterface` |
| 모드 | `UHarmoniaModSubsystem`, `UHarmoniaAssetOverrideSubsystem` |
| 월드 생성 | `UHarmoniaWorldGeneratorSubsystem` |
| 치트 | `UHarmoniaCheatManager` (Editor/Dev 전용) |
| 스토리 | `UHarmoniaDialogueComponent`, `UHarmoniaJournalSubsystem`, `UHarmoniaCutsceneManager` |
| 로컬라이제이션 | `UHarmoniaLocalizationSubsystem`, `UHarmoniaLocalizationBlueprintLibrary` |
| 게임 서비스 | `UHarmoniaGameService` (Achievement, Cloud Save, Leaderboard) |

---

## 🔌 주요 인터페이스 경로

| 인터페이스 | 경로 |
|-----------|------|
| `IHarmoniaTeamAgentInterface` | `Definitions/HarmoniaTeamSystemDefinitions.h` |
| `IHarmoniaSaveInterface` | `HarmoniaLoadManager/Public/Save/HarmoniaSaveInterface.h` |
| `IHarmoniaInteractableInterface` | `Interfaces/HarmoniaInteractableInterface.h` |
| `IHarmoniaAdminInterface` | `Interfaces/HarmoniaAdminInterface.h` |
| `IHarmoniaMonsterInterface` | `Monsters/HarmoniaMonsterInterface.h` |

---

## 📊 주요 데이터 구조체

```cpp
// 무기 데이터
FHarmoniaMeleeWeaponData

// 콤보 공격 데이터
FComboAttackData, FComboAttackStep

// 퀘스트 데이터
FQuestData, FQuestPhase, FQuestCondition, FQuestObjective

// 던전 데이터
FHarmoniaDungeonData, FDungeonStageData

// 제작 레시피
FCraftingRecipeData, FCraftingIngredient

// 팀 식별자
FHarmoniaTeamIdentification

// 회복 아이템
FRechargeableItemData
```

---

## 🔧 코딩 컨벤션

### 네이밍 규칙

| 타입 | 패턴 | 예시 |
|------|------|------|
| 컴포넌트 | `UHarmonia[기능명]Component` | `UHarmoniaMeleeCombatComponent` |
| 어빌리티 | `UHarmoniaGameplayAbility_[기능명]` | `UHarmoniaGameplayAbility_Dodge` |
| 서브시스템 | `UHarmonia[기능명]Subsystem` | `UHarmoniaQuestSubsystem` |
| 데이터 구조체 | `FHarmonia[데이터명]Data` | `FHarmoniaDungeonData` |
| 인터페이스 | `IHarmonia[인터페이스명]Interface` | `IHarmoniaTeamAgentInterface` |

### 파일 구성

| 파일 타입 | 패턴 | 용도 |
|----------|------|------|
| Definitions | `Harmonia[시스템명]Definitions.h` | Enum, Struct 정의 |
| Component | `Harmonia[기능명]Component.h/.cpp` | 컴포넌트 구현 |
| Library | `UHarmonia[기능명]Library` | 정적 유틸리티 함수 |

### 빌드 제한

```cpp
// Shipping 빌드에서 제외할 코드
#if !UE_BUILD_SHIPPING
// 치트, 디버그 코드
#endif
```

---

## 📝 작업 시 참고사항

### 새 기능 추가 시

1. `Source/HarmoniaKit/Public/` 에 헤더 파일 생성
2. `Source/HarmoniaKit/Private/` 에 구현 파일 생성
3. 필요한 경우 Definitions 파일에 데이터 구조체 추가
4. `Docs/Systems/` 에 문서 작성

### GAS 통합 시

- `UHarmoniaAttributeSet` 사용
- `UHarmoniaGameplayAbility` 기반 어빌리티 생성
- GameplayTag는 `HarmoniaKit` 네임스페이스 사용

### Gameplay Ability 태그 설정 시

> ⚠️ **중요**: 태그 설정 전 반드시 `Config/DefaultGameplayTags.ini`에서 중복 여부 확인!

- **태그 설정 가이드**: `Docs/HarmoniaKit_Complete_Documentation.md` 섹션 17 참조
- 모든 어빌리티 태그는 **블루프린트 또는 파생 클래스**에서 설정
- 태그 네이밍: `State.X` 패턴 사용 (예: `State.Dodging`, `State.Combat.Attacking`)
- ❌ `Character.State.X` 패턴 지양

**필수 확인 사항**:
1. `Config/DefaultGameplayTags.ini` - 기존 태그 정의
2. `Config/DefaultGameplayTags.ini`에 연결된 DataTable들 (예: `DT_HarmoniaCharacterTypes`)
3. 프로젝트 내 다른 어빌리티에서 사용 중인 태그

### 네트워크 복제 시

- `UPROPERTY(Replicated)` 또는 `UPROPERTY(ReplicatedUsing=OnRep_XXX)` 사용
- `GetLifetimeReplicatedProps()` 구현 필수
- 서버 권한 함수는 `UFUNCTION(Server, Reliable)` 사용

### 블루프린트 노출 시

- `UFUNCTION(BlueprintCallable, Category = "Harmonia|[카테고리명]")`
- `UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Harmonia|[카테고리명]")`

---

## ⚡ 빠른 코드 예시

### 근접 공격 설정

```cpp
// 캐릭터에 컴포넌트 추가
UHarmoniaMeleeCombatComponent* MeleeCombatComp = CreateDefaultSubobject<UHarmoniaMeleeCombatComponent>(TEXT("MeleeCombatComp"));

// 공격 실행
MeleeCombatComp->TryLightAttack();
MeleeCombatComp->TryHeavyAttack();
MeleeCombatComp->TryDodge();
```

### 팀 시스템 사용

```cpp
UHarmoniaTeamManagementSubsystem* TeamSubsystem = GetWorld()->GetSubsystem<UHarmoniaTeamManagementSubsystem>();

// 팀 생성
FHarmoniaTeamIdentification Team = TeamSubsystem->CreateFaction(FText::FromString("TeamName"), EHarmoniaTeamAttitude::Hostile, FLinearColor::Red);

// 관계 설정
TeamSubsystem->MakeTeamsEnemies(Team1, Team2);
```

### 퀘스트 진행

```cpp
UHarmoniaQuestComponent* QuestComp = Character->FindComponentByClass<UHarmoniaQuestComponent>();
QuestComp->AcceptQuest("QuestID");
QuestComp->UpdateQuestProgress("QuestID", "ObjectiveID", 1);
```

### 락온 시스템 사용

```cpp
// 캐릭터에 컴포넌트 추가
UHarmoniaLockOnComponent* LockOnComp = CreateDefaultSubobject<UHarmoniaLockOnComponent>(TEXT("LockOnComp"));

// 락온 토글
LockOnComp->ToggleLockOn();

// 타겟 전환
LockOnComp->SwitchTargetLeft();
LockOnComp->SwitchTargetRight();

// 상태 확인
bool bIsLocked = LockOnComp->IsLockedOn();
AActor* Target = LockOnComp->GetCurrentTarget();
```

### 회피 어빌리티 설정

```cpp
// 롤 타입별 파라미터 (장비 무게에 따라 자동 결정)
// Light (0-30%): 거리 500, I-Frame 0.4초
// Medium (30-70%): 거리 400, I-Frame 0.3초
// Heavy (70%+): 거리 300, I-Frame 0.2초
```

---

## 📚 상세 문서 위치

| 문서 | 경로 |
|------|------|
| **통합 문서** | `Docs/HarmoniaKit_Complete_Documentation.md` |
| **Ability 태그 가이드** | `Docs/HarmoniaKit_Complete_Documentation.md` 섹션 17 |
| 전투 시스템 | `Docs/Systems/CombatSystem.md` |
| 근접 전투 | `Docs/Systems/MeleeCombatSystem.md` |
| 콤보 공격 | `Docs/Systems/ComboAttackSystem.md` |
| 퀘스트 | `Docs/Systems/QuestSystem.md` |
| 던전/레이드 | `Docs/Systems/DungeonRaidSystem.md` |
| 팀 시스템 | `Docs/TeamSystemUsageGuide.md` |
| 생활 컨텐츠 | `LifeContents_Systems_Summary.md` |
| **태그 설정 파일** | `Config/DefaultGameplayTags.ini` |

---

## ⚠️ 알려진 이슈 및 제한사항

1. **HarmoniaMonsterAnimationInterface.cpp 누락**: 순수 인터페이스로 구현 파일이 필요 없음 (UINTERFACE 매크로 자동 생성)

2. **플랫폼 제한**: 현재 Win64만 지원. 다른 플랫폼 추가 시 `HarmoniaKit.uplugin`의 `SupportedTargetPlatforms` 수정 필요

3. **Shipping 빌드 제외 코드**: `HarmoniaCheatManager`, `HarmoniaCheatLibrary`는 Shipping 빌드에서 자동 제외됨

---

**최종 업데이트**: 2025-11-26
````
